# 钢材产业基本面量化系统：Agent 协作规则

## 项目目标

本项目以螺纹钢为主要交易研究对象，建立日线及以上周期的钢材产业基本面量化研究系统。系统研究总钢材资源、卷螺品种分配、热卷与螺纹供需、原料、库存、现货、出口、宏观、情绪和价格结构。

当前处于阶段 A：无代码模型建设与数据覆盖核查。当前工作重点是建立可解释、可验证、可追溯的研究体系，不直接开发自动交易或追求参数优化。

## 开始工作前

开始任何任务前，按以下顺序阅读：

1. `README.md`
2. `钢材产业基本面量化系统_项目指导与实施手册_V0.2.md`
3. `版本/模型版本记录.md`
4. 与当前任务直接相关的阶段 A 文件、研究报告或数据字典

如果任务涉及 Wind 数据，先阅读项目中的 Wind 探查报告，并遵守当前会话提供的 Wind skill 规则。不要直接凭记忆猜测 Wind 指标代码、字段、单位或接口参数。

## 目录职责

- `阶段A/`：策略总纲、产业因果模型、状态变量、市场状态和模型信任机制。
- `阶段A/产业因果专题/`：A02.1—A02.6 的专项因果研究。
- `数据/原始/`：原始事实和来源记录。不得写入状态判断或交易结果。
- `数据/加工/`：由原始事实计算出的派生指标，并记录输入观测 ID 和规则版本。
- `数据/状态/`：按判断时点形成的状态输出。
- `研究/`：历史案例、假设验证、预测快照和接口探查。
- `交易/`：实际交易记录，独立于基本面数据库。
- `outputs/阶段A_框架初版/`：A03、A07、A08、A09 工作簿的正式维护位置。
- `版本/`：模型版本和框架变更记录。

## 核心研究规则

- `State != Position`。市场状态、模型判断和实际持仓必须分开记录。
- 允许输出 `Neutral` 和 `Unknown`。缺失数据不得自动解释成中性。
- 任何指标必须区分 `Level`、`ΔLevel`、`Δ²Level` 和历史/季节性位置。
- 不给库存下降、高基差、低利润或增仓上涨永久固定的多空含义；指标含义取决于市场状态和产业机制。
- 基本面与价格背离是正式状态。持续背离应降低模型信任，不能机械逆势加仓。
- 先写清经济因果和反例，再选择数据和计算方法；禁止先找赚钱因子再补解释。
- 新经验、新规则和新参数先进入 `A09_研究假设库.xlsx`，完成历史验证和样本外验证后，才可进入正式模型。
- 模型逻辑是否正确与交易是否盈利必须分开评价，使用 A08 错误归因记录。

## 数据时间与未来函数

所有原始记录至少保留：

- `data_date`：数据归属日期或统计期。
- `available_date`：该版本实际可获得的日期和时间。
- `recorded_at`：本项目记录该版本的时间。
- `revision_id`：修订版本或来源版本。

任何历史判断只能使用判断时点之前已经可获得的数据。`updateDate`、网页更新时间或当前查询时间不能直接替代每条观测的 `available_date`。如果发布时间未知，必须标记为未知，并禁止将该数据用于严格历史回测。

日、周、月数据保持原生频率。不要把前值填充后的周度数据当作日度新信息，也不要把不同统计周、地区、品种或样本范围直接相减。不同单位和不同口径的数据不得未经说明拼接。

## Wind 数据工作纪律

- 先搜索指标元信息，再按代码取短区间样本；不要未经必要性全量拉取。
- 保存每次请求和完整回执，至少记录指标代码、名称、单位、频率、来源、历史起止日期、请求日期范围和返回状态。
- “能搜索到代码”不等于“当前账号有权限取数”。必须实际取一个小样本验证。
- “无权限”与“未找到准确口径”分开记录，不能笼统写成“Wind 没有”。
- 混合日、周、月指标时使用明确 `beginDate`/`endDate`，并核对每条序列的返回日期数量和日期间隔。
- 严格保留 Wind 返回的单位和量级，不自行换算或修正浮点尾差。
- 产业数据须核实样本范围、统计周、地区、品种、来源和更新时间。Wind、钢联、富宝、百川等不同来源不能默认同口径。
- 当前账号已知的权限缺口和口径限制见 `研究/Wind接口覆盖探查_20260919/` 与 `研究/Wind专项补充探查_20260919/`，接入新数据前先查阅。

## 文件修改和协作边界

- 开始前检查目标文件是否已存在，优先增量修改，避免创建同名副本。
- 不覆盖用户已有数据，不删除研究回执、历史快照或版本记录。
- 多个 agent 并行工作时，按文件或专题划分范围；不要同时修改同一个 Markdown、CSV 或工作簿。
- 需要修改共享规则时，先检查是否影响 A01、A04、A05、A06 和现有研究报告，并在版本记录中说明。
- 未经明确要求，不执行自动交易、真实下单、外部发布、删除数据或大规模重写。

## 跨 Agent 交接

跨 Codex、Antigravity 或其他 agent 的任务状态统一写入 `交接/`：开始前阅读 `交接/当前状态.md`、`交接/任务队列.md` 和 `交接/变更日志.md`；开始工作时认领任务；结束时更新状态、队列和变更日志。聊天消息不能作为唯一交接记录。

当前目录尚未使用 Git worktree。多个 agent 可以同时读取项目，但同一个文件不能同时由两个 agent 修改。需要并行开发时，按文件或专题划分任务；如需分支隔离，先完成 Git 初始化，再使用独立分支或 worktree。

## 交付要求

完成任务后必须报告：

1. 修改或新增的文件及其用途。
2. 使用的数据来源、查询日期和重要口径限制。
3. 完成的验证，包括链接、字段、日期、公式或取数回执检查。
4. 尚未解决的缺口、权限问题和待验证假设。
5. 如果改变模型规则，更新 `版本/模型版本记录.md`；如果只是框架或探查材料，说明为框架版本变更，不得冒充正式模型升级。

## 当前优先任务

按固定顺序推进：

1. 完善 `A02.1 总钢材资源池`。
2. 核实 Wind 与自有数据的可获得性、统计口径和发布时间。
3. 处理废钢、铁矿、焦煤、焦炭、热卷需求和出口接单的数据缺口。
4. 用一个历史案例跑通“原始事实 → 加工指标 → 状态判断 → 预测快照 → 错误归因 → 研究假设”的闭环。
5. 在阶段 A 完成前，不进入复杂机器学习、参数优化或自动交易。
